//Escreva um programa em C que leia a nota de um aluno e informe se ele foi aprovado, reprovado ou está em recuperação. Para ser aprovado ele deve tirar ao menos 6, para estar reprovado ele deve tirar menos que 3 e para estar em recuperação ele deve estar entre 3 e 6.
#include <stdio.h>

int main()
{
    float notas;
    printf("Digite sua nota de 0 a 10: ");
    scanf("%f",&notas);
    if (notas<3)
        printf("Reprovado");
    if (notas>=3 && notas<6)
        printf("Está em recuperação");
        if (notas>=6)
        printf("Aprovado");
    return 0;
}
